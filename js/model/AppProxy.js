var model = model || {};

function AppProxy() 
{
	//-------------------------------------------------------------------------
	//
	// Properties
	//
	//-------------------------------------------------------------------------

	this.activePage = enums.pages.home;
}

model.AppProxy = model.AppProxy || new AppProxy();